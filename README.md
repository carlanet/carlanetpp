# carla_inet

```sh
git clone --no-checkout git@github.com:christianquadri/carla_inet.git
cd carla_inet/
git sparse-checkout set --cone
git checkout main
git sparse-checkout set [carla_side|omnet_side]
```
